PORT=9090
ALLOWED_ORIGINS=http://localhost:8080

# Local PostgreSQL (Docker)
DATABASE_URL=postgres://postgres:postgres@localhost:5433/sihuni

# Auth — empty = dev bypass (mock user injected)
SUPABASE_JWT_SECRET=
SUPABASE_SERVICE_KEY=
SUPABASE_URL=

# Payment / Email — not needed for dev
XENDIT_SECRET_KEY=
XENDIT_WEBHOOK_TOKEN=
RESEND_API_KEY=

# Dev
APP_ENV=development
ADMIN_SECRET=dev-admin-secret
CRON_SECRET=dev-cron-secret

JWT_SECRET=dev-secret-change-in-prod
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h
